<html>
    <head>
        <%- include('partials/bootstrap') %>
        <title>Home</title>
        <style>
            .bread-name {
                font-weight: bold;
            }

            .bread-price {
                color: #f90f36;
                font-weight: bold;
            }
        </style>
    </head>
    <body>
        <%- include('partials/navbar') %>
        <div class="container">
            <div class="mb-4">
                <h1>Beli Buku</h1>
                <div class="row">
                    <div class="col-md-4 text-center">
                        <img src="<%= bread.image %>" class="img-fluid" alt="bread image">
                    </div>
                    <div class="col-md-8">
                        <h1 class="bread-name"><%= bread.name %></h1>
                        <h2 class="bread-chef"><%= bread.chef %></h2>
                        <h3 class="bread-price"><%= toIndonesianRupiah(bread.price) %></h3>
                        <form class="my-5" action="" method="POST">
                            <div class="form-group row">
                                <label class="col-sm-1 col-form-label" for="amount">Jumlah: </label>
                                <div class="col-sm-11">
                                    <input class="form-control" type="number" name="amount" id="amount" placeholder="Jumlah" value="1" min="1" max="99">
                                </div>
                            </div>
                            <input class="btn btn-lg btn-primary" type="submit" value="beli">
                        </form>
                    </div>
                </div>
                <div class="d-flex justify-content-center">
                    <div class="bread-item p-3 my-3 text-center">
                    </div>
                </div>
            </div>
        </div>
    </body>
    <foot>
        <%- include('partials/footer') %>
    </foot>
</html>
